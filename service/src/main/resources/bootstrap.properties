# Include all endpoints
management.endpoints.web.exposure.include=*
# Exclude specifics
management.endpoints.web.exposure.exclude=env

spring.cloud.config.uri=http://localhost:8081/
#profile profile name can be used in properties file service-one-shit
#spring.profiles.active=shit


#the service will be registered with this name on the eureka serve
spring.application.name=service-one
server.port=8081

